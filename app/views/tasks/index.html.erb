<h1>My tasks</h1>

<% @tasks.each do |task|%>
    <div> <input type="checkbox" <%= task.completed ? "checked" : "checked: 'false'" %>>  <%= link_to task.title, task_path(task) %> </div>
<% end %>


<%= link_to "Add a new task", new_task_path %>